import {StyleSheet} from 'react-native';

const COLORS = {
  textPrimary: '#FFFEF9',
  textBold: '#FFFEF9',
  background: '#92BAF9',
};

export default StyleSheet.create({
  date: {
    padding: 10,
    fontSize: 16,
    color: COLORS.textPrimary,
    textAlign: 'center',
  },
});
